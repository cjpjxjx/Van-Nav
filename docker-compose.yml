services:
  van-nav:
    build:
      context: .
      dockerfile: Dockerfile
    image: cjpjxjx/Van-Nav:latest
    container_name: van-nav
    restart: unless-stopped
    ports:
      - 6412:6412
    environment:
      - TZ=Asia/Shanghai
    volumes:
      - ./data:/app/data
